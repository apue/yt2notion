"""Subtitle file processing: parsing, cleaning, chunking with timestamps."""

from __future__ import annotations

# TODO: Implement
# - parse_subtitle_file(path) -> list of (timestamp_seconds, text) tuples
# - Support both SRT and VTT formats
# - clean_transcript(entries) -> cleaned text with timestamp markers
# - chunk_by_time(entries, chunk_minutes=2) -> list of chunks with start timestamps
