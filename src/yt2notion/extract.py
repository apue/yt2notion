"""YouTube video subtitle and metadata extraction via yt-dlp."""

from __future__ import annotations

# TODO: Implement
# - extract_subtitles(url, config) -> Path to best subtitle file
# - extract_metadata(url) -> VideoMeta
# - Subtitle priority: zh-Hans > zh-Hant > en (manual) > en (auto)
# - Use --cookies-from-browser for authenticated access
