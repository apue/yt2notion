请翻译下面的课程内容。每章中的所有 block 共同构成该章上下文；翻译每个 block 时应利用同章前后文保持指代和术语一致，但每个输出只能包含对应 block 的译文。

返回格式必须是一个扁平 JSON 数组，按章节和 block 的输入顺序排列：

[
  {"block_id": "c001-b001", "translation": "对应语义块的完整中文译文"}
]

不得合并、遗漏、增加或重排 block_id。

输入：

{source_json}
